from setuptools import setup

setup(
    name='rm79684',
    packages=['rm79684'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
    setup_requires=[
        'pytest-runner',
    ],
    tests_require=[
        'pytest',
    ],
)